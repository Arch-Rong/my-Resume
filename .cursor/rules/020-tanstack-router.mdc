---
description: TanStack Router 文件路由与页面约定
globs: src/routes/**/*
alwaysApply: false
---

# TanStack Router

## 路由文件

- 路径：`src/routes/`，文件名即 URL 段
- 使用 `createFileRoute('/path')({ component, ... })`
- 链接用 `<Link to="...">` 或 `useNavigate()`，避免裸 `<a href>`（内部路由）

## 布局

- 全局壳：`src/routes/__root.tsx`（Header、样式、主题脚本）
- 不要编辑 `src/routeTree.gen.ts`

## 新增路由后

运行 `pnpm dev` 或 `pnpm build` 让 plugin 重新生成 route tree。

## 页面结构示例

```tsx
import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/resumes")({
	component: ResumesPage,
});

function ResumesPage() {
	return <main className="page-wrap">...</main>;
}
```
